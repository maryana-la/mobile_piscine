<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MobileWeatherApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="CustomSearchViewTheme" parent="Theme.MaterialComponents.Light">
        <item name="colorControlNormal">@color/ivory</item>
        <item name="android:textColor">@color/ivory</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/blue</item>
    </style>


    <style name="Theme.MobileWeatherApp" parent="Base.Theme.MobileWeatherApp" />
</resources>